{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "10.22.0"
  },
  "scripts": {
    "test": "lerna run test",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run tsc --stream",
    "build:watch": "lerna run watch --stream",
    "pub": "lerna publish --skip-git",
    "del": "lerna exec -- rm -rf ./dist"
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "jest": "^24.9.0",
    "lerna": "^3.17.0",
    "prettier": "^1.18.2",
    "ts-jest": "^24.2.0",
    "typescript": "^3.6.4"
  }
}
